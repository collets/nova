{
  "name": "@org/personal-site-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/personal-site-e2e/src",
  "projectType": "application",
  "tags": ["type:e2e", "scope:personal-site"],
  "targets": {
    "e2e-smoke": {
      "executor": "nx:run-commands",
      "dependsOn": ["@org/personal-site:build"],
      "inputs": [
        "default",
        "^production",
        {
          "externalDependencies": ["@playwright/test"]
        },
        {
          "env": "CI"
        }
      ],
      "outputs": ["{workspaceRoot}/dist/.playwright/apps/personal-site-e2e"],
      "options": {
        "cwd": "apps/personal-site-e2e",
        "command": "PLAYWRIGHT_WEB_SERVER_PORT=4201 playwright test --grep @smoke"
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "dependsOn": ["@org/personal-site:build"],
      "inputs": [
        "default",
        "^production",
        {
          "externalDependencies": ["@playwright/test"]
        },
        {
          "env": "CI"
        }
      ],
      "outputs": ["{workspaceRoot}/dist/.playwright/apps/personal-site-e2e"],
      "options": {
        "cwd": "apps/personal-site-e2e",
        "command": "PLAYWRIGHT_WEB_SERVER_PORT=4202 playwright test --grep \"@regression|@smoke\""
      }
    },
    "e2e-headed": {
      "executor": "nx:run-commands",
      "dependsOn": ["@org/personal-site:build"],
      "options": {
        "cwd": "apps/personal-site-e2e",
        "command": "PLAYWRIGHT_WEB_SERVER_PORT=4200 playwright test --headed"
      }
    },
    "e2e-ui": {
      "executor": "nx:run-commands",
      "dependsOn": ["@org/personal-site:build"],
      "continuous": true,
      "options": {
        "cwd": "apps/personal-site-e2e",
        "command": "PLAYWRIGHT_WEB_SERVER_PORT=4200 playwright test --ui"
      }
    }
  }
}
